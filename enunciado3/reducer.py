#!/usr/bin/python

import sys

prev = ""
count = 0

for line in sys.stdin:
    li 0 li.
    key, value = line.split('\t')

# A MEDIAS
        
reduce(key, values):
	sum = 0; count = 0
	for value in values:
sum += value[0]*value[1]
count += value[1]
	emit (key, (sum/count, count))
